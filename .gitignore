/.phpunit.cache
/node_modules
**/node_modules
**/node_modules/**
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
yarn-debug.log
.pnp
.pnp.js
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Vercel

.npm
.node_repl_history

# Testing
coverage/

# Production
build/
public/build/
public/hot/
public/storage/
dist/

# Environment variables
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db
.cursor/

# Backup & scratch files
*.backup
*.tst
.env
.env.*
!.env.example
.vercel
.env*

# Kiro local MCP config (may contain machine-specific / sensitive settings)
.kiro/settings/mcp.json

# design-sync (claude.ai/design)
# Staged converter, generated bundle, and machine-local verification state.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
# The repo-wide `build/` rule above also matches .design-sync/build/, which holds
# committed sync inputs (the entry barrel, the CSS build script, tokens).
# Re-include the directory, then re-ignore only what that script generates.
!.design-sync/build/
.design-sync/build/app.compiled.css
.design-sync/build/tailwind.out.css

# Recorded Amadeus certification evidence (scripts/record-amadeus-fixture.mjs).
# Regenerated per run, and carries the office id and WS username of whoever
# recorded it. The pack goes to Amadeus directly, not through the repo.
tests/fixtures/amadeus/certification/
