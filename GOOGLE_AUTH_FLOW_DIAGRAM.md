# Google Auth Flow - Visual Guide

## 🔄 Complete Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     USER AUTHENTICATION FLOW                      │
└─────────────────────────────────────────────────────────────────┘

Step 1: User lands on test page
┌────────────────────┐
│   Browser          │
│  /google-auth-test │
│                    │
│  [Sign in with     │
│   Google button]   │
└────────────────────┘
         │
         │ User clicks button
         ↓

Step 2: Initiate OAuth flow
┌────────────────────┐
│  GoogleAuthTest    │
│  Component         │
│                    │
│  supabase.auth     │
│  .signInWithOAuth  │
└────────────────────┘
         │
         │ Redirects to ↓
         ↓

Step 3: Google login
┌────────────────────┐
│  Google OAuth      │
│  accounts.google   │
│                    │
│  [Select Account]  │
│  [Allow Access]    │
└────────────────────┘
         │
         │ User authenticates
         ↓

Step 4: OAuth callback
┌────────────────────┐
│  Supabase Server   │
│  /auth/v1/callback │
│                    │
│  Creates session   │
│  Generates tokens  │
└────────────────────┘
         │
         │ Redirects with session
         ↓

Step 5: Return to app
┌────────────────────┐
│  Browser           │
│  /google-auth-test │
│                    │
│  Session detected  │
│  User info shown   │
└────────────────────┘
```

---

## 🏗️ Architecture Overview

```
┌──────────────────────────────────────────────────────────────┐
│                      FRONTEND (React)                         │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  GoogleAuthTest Component                            │    │
│  │  - Handles UI                                        │    │
│  │  - Manages auth state                                │    │
│  │  - Displays user info                                │    │
│  └───────────────────┬──────────────────────────────────┘    │
│                      │                                        │
│                      │ Uses                                   │
│                      ↓                                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  supabaseClient.js                                   │    │
│  │  - Configures Supabase                               │    │
│  │  - Exports client instance                           │    │
│  └───────────────────┬──────────────────────────────────┘    │
│                      │                                        │
└──────────────────────┼────────────────────────────────────────┘
                       │
                       │ API Calls
                       ↓
┌──────────────────────────────────────────────────────────────┐
│                   SUPABASE BACKEND                            │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Authentication Service                              │    │
│  │  - OAuth flow management                             │    │
│  │  - Session management                                │    │
│  │  - Token handling                                    │    │
│  └───────────────────┬──────────────────────────────────┘    │
│                      │                                        │
│                      │ Integrates with                        │
│                      ↓                                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Google OAuth Provider                               │    │
│  │  - Client ID & Secret                                │    │
│  │  - Redirect URIs                                     │    │
│  └───────────────────┬──────────────────────────────────┘    │
│                      │                                        │
└──────────────────────┼────────────────────────────────────────┘
                       │
                       │ Validates with
                       ↓
┌──────────────────────────────────────────────────────────────┐
│                      GOOGLE OAUTH                             │
├──────────────────────────────────────────────────────────────┤
│  - User authentication                                        │
│  - Consent screen                                             │
│  - Returns user profile                                       │
└──────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │
       │ 1. Request /google-auth-test
       ↓
┌─────────────┐
│  React App  │
└──────┬──────┘
       │
       │ 2. Load GoogleAuthTest component
       ↓
┌─────────────────┐
│ GoogleAuthTest  │
└──────┬──────────┘
       │
       │ 3. Check for existing session
       ↓
┌─────────────────┐
│ supabaseClient  │
└──────┬──────────┘
       │
       │ 4. Query Supabase
       ↓
┌─────────────┐
│  Supabase   │───── No session? ────→ Show login button
└──────┬──────┘
       │
       │ Has session?
       ↓
┌─────────────┐
│ Display     │
│ User Info   │
└─────────────┘
```

---

## 🔐 Security Flow

```
1. CLIENT INITIATES
   Browser → supabaseClient → Supabase

2. SUPABASE REDIRECTS
   Supabase → Google OAuth → User

3. USER AUTHENTICATES
   User → Google → Consent

4. GOOGLE RETURNS
   Google → Supabase (with auth code)

5. SUPABASE PROCESSES
   Supabase:
   - Validates auth code
   - Creates session
   - Generates JWT tokens
   - Stores session

6. CLIENT RECEIVES
   Supabase → Browser (with session)
   - Access token (JWT)
   - Refresh token
   - User metadata

7. ONGOING
   Browser:
   - Stores tokens (localStorage)
   - Auto-refreshes when needed
   - Validates on each request
```

---

## 🗂️ File Structure

```
JETSET13/
│
├── frontend/
│   ├── lib/
│   │   └── supabaseClient.js ←─────┐
│   │                                │
│   ├── components/                  │
│   │   ├── GoogleAuthTest.jsx ──────┤ Imports
│   │   └── README-GoogleAuthTest.md │
│   │                                │
│   └── App.jsx ──────────────────────┘ Defines route
│
├── Documentation/
│   ├── GOOGLE_AUTH_SETUP_GUIDE.md
│   ├── GOOGLE_AUTH_QUICK_START.md
│   ├── GOOGLE_AUTH_TEST_SUMMARY.md
│   └── GOOGLE_AUTH_FLOW_DIAGRAM.md (this file)
│
└── Scripts/
    └── test-google-auth-setup.js
```

---

## 🔄 Session Lifecycle

```
SESSION CREATION
┌────────────────────────────────┐
│ User signs in with Google       │
└───────────────┬────────────────┘
                │
                ↓
┌────────────────────────────────┐
│ Supabase creates session        │
│ - Access token (JWT)            │
│ - Refresh token                 │
│ - Expiry: 1 hour                │
└───────────────┬────────────────┘
                │
                ↓
┌────────────────────────────────┐
│ Browser stores in localStorage  │
└───────────────┬────────────────┘
                │
                ↓

SESSION USAGE
┌────────────────────────────────┐
│ Every page load/request         │
└───────────────┬────────────────┘
                │
                ↓
┌────────────────────────────────┐
│ Check if token expired          │
└───────────┬───────┬────────────┘
            │       │
    Valid   │       │ Expired
            │       │
            ↓       ↓
┌───────────────┐ ┌───────────────┐
│ Use existing  │ │ Auto-refresh  │
│ token         │ │ using refresh │
└───────────────┘ │ token         │
                  └───────┬───────┘
                          │
                          ↓
                  ┌───────────────┐
                  │ Get new       │
                  │ access token  │
                  └───────────────┘

SESSION TERMINATION
┌────────────────────────────────┐
│ User clicks "Sign Out"          │
└───────────────┬────────────────┘
                │
                ↓
┌────────────────────────────────┐
│ supabase.auth.signOut()         │
└───────────────┬────────────────┘
                │
                ↓
┌────────────────────────────────┐
│ Clear tokens from localStorage  │
└───────────────┬────────────────┘
                │
                ↓
┌────────────────────────────────┐
│ Invalidate session on server    │
└────────────────────────────────┘
```

---

## 🎯 Component State Flow

```
GoogleAuthTest Component State

Initial Load
└── loading: true
    user: null
    session: null
    error: null

After getSession()
├── If no session:
│   └── loading: false
│       user: null
│       session: null
│
└── If session exists:
    └── loading: false
        user: { id, email, ... }
        session: { access_token, ... }

User clicks "Sign in"
└── loading: true
    Redirects to Google...

After OAuth return
└── onAuthStateChange triggered
    ├── session: { ... }
    ├── user: { ... }
    └── loading: false

User clicks "Sign out"
└── loading: true
    ↓
    signOut()
    ↓
    loading: false
    user: null
    session: null
```

---

## 🌐 URL Flow

```
DEVELOPMENT
┌─────────────────────────────────────────┐
│ Start: http://localhost:5173             │
└────────────────┬────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────┐
│ Test page:                               │
│ http://localhost:5173/google-auth-test   │
└────────────────┬────────────────────────┘
                 │
                 │ Click "Sign in"
                 ↓
┌─────────────────────────────────────────┐
│ Redirects to:                            │
│ https://accounts.google.com/o/oauth2/... │
└────────────────┬────────────────────────┘
                 │
                 │ User authenticates
                 ↓
┌─────────────────────────────────────────┐
│ Callback to:                             │
│ https://qqmagqwumjipdqvxbiqu             │
│     .supabase.co/auth/v1/callback        │
└────────────────┬────────────────────────┘
                 │
                 │ With session
                 ↓
┌─────────────────────────────────────────┐
│ Returns to:                              │
│ http://localhost:5173/google-auth-test   │
│ (now authenticated)                      │
└─────────────────────────────────────────┘
```

---

## ⚙️ Configuration Points

```
1. GOOGLE CLOUD CONSOLE
   ┌──────────────────────────┐
   │ OAuth 2.0 Client         │
   │ - Client ID              │
   │ - Client Secret          │
   │ - Authorized URIs        │
   └──────────────────────────┘
              ↓
2. SUPABASE DASHBOARD
   ┌──────────────────────────┐
   │ Google Provider          │
   │ - Enable toggle          │
   │ - Client ID (paste)      │
   │ - Client Secret (paste)  │
   └──────────────────────────┘
              ↓
3. YOUR CODE
   ┌──────────────────────────┐
   │ supabaseClient.js        │
   │ - Supabase URL           │
   │ - Anon Key               │
   │ - Auth options           │
   └──────────────────────────┘
              ↓
4. COMPONENT
   ┌──────────────────────────┐
   │ GoogleAuthTest.jsx       │
   │ - Uses supabaseClient    │
   │ - Handles auth flow      │
   │ - Displays results       │
   └──────────────────────────┘
```

---

## 🧪 Testing Flow

```
1. SETUP
   └─→ Configure Google OAuth
   └─→ Enable in Supabase
   └─→ Verify redirect URIs

2. START SERVER
   └─→ npm run dev
   └─→ Server running on :5173

3. NAVIGATE
   └─→ Open browser
   └─→ Go to /google-auth-test

4. TEST LOGIN
   └─→ Click "Sign in with Google"
   └─→ Select Google account
   └─→ Grant permissions

5. VERIFY
   └─→ Check user info displays
   └─→ Verify session data
   └─→ Confirm profile picture

6. TEST LOGOUT
   └─→ Click "Sign Out"
   └─→ Verify return to initial state
   └─→ Confirm session cleared

7. TEST PERSISTENCE
   └─→ Refresh page after login
   └─→ Verify still logged in
   └─→ Close and reopen tab
   └─→ Verify session persists
```

---

## 📝 Summary

This visual guide shows:
- ✅ Complete authentication flow
- ✅ Architecture overview
- ✅ Data flow between components
- ✅ Security flow
- ✅ File structure
- ✅ Session lifecycle
- ✅ Component state management
- ✅ URL flow
- ✅ Configuration points
- ✅ Testing process

Everything is set up and ready to test!
